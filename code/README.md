# Código

Mové acá tu paquete `Code/` (spacetimes/, shell/, integrators/, diagnostics/).

Por ahora contiene:
- `initial_conditions_map.py` — genera el mapa de condiciones iniciales
  (retrato de fase + potencial efectivo) sobre el núcleo general f_±.
  Salida -> ../figures/initial_conditions_map.png
